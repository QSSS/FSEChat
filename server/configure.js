'use strict';

var express = require('express'),
    path = require('path'),
    config = require('../config');
/**
 * Express configuration
 */

var init = function(app) {

}

module.exports = {
  init: init
};
